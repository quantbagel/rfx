# Baseline Files

- `rfxjit_microkernels_cpu.json`: tracked CPU baseline used by CI soft perf gate.

GPU baselines (`cuda`, `metal`) are intentionally deferred until dedicated
runner coverage is added to CI.
